<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EJS nas</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .content {
            display: flex;
            flex-direction: column;
            align-items: center;
            max-width: 80%;
            height: 80%;
            border: 1px solid #ccc;
            overflow: hidden;
            padding: 20px;
        }
        .scroll-box {
            flex: 1;
            overflow-y: auto;
            border: 1px solid #ccc;
            padding: 10px;
            margin-top: 10px;
            max-width: 100%; /* 추가된 속성 */
            width: 600px; /* 원하는 너비 값으로 변경 */
        }
    </style>
        <script>
        function downloadFile() {
            const fileContents = `<%= fileContent %>`;
            const filename = 'download.txt';

            const blob = new Blob([fileContents], { type: 'text/plain' });
            const link = document.createElement('a');
            link.href = window.URL.createObjectURL(blob);
            link.download = filename;
            link.click();
        }
    </script>
</head>
<body>
    <div class="content">
        <h2><%= iprend %></h2>
        <div class="scroll-box">
            <pre><%= fileContent %></pre>
        </div>
        Server folder : <strong><%= rend %></strong>
        <br>
        <button onclick="downloadFile()">Download File</button>
        <br>
        <hr>
        Now path : <strong><%= currentDir %></strong>
    </div>

</body>
</html>